const fs = require("fs");
const data = require("../data/config.json");

module.exports = {

  saveLocal: function() {
    fs.writeFile("./node_modules/discord-giveaway-easy/src/CompleteGiveaway/data/config.json", JSON.stringify(data, null, 4), (err) => {
      if (err) console.log(err)
  })
  }


}
