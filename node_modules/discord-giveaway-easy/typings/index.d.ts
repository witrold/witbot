'use strict';

import GiveawaysManager from "../src/CompleteGiveaway/module/system/manager/GiveawayManager";
import { GiveawaysStartOptions } from "../src/CompleteGiveaway/module/system/utils/Constants";

import { Channel, Client, Message } from "discord.js";

export declare class giveawaysManager extends GiveawaysManager {

  constructor(client: Client) {
    super(client)
  }

   /**
   * Start the giveaway with the manager.
   * @param {Discord.Channel} channel 
   * @param {GiveawaysStartOptions} options 
   * @example
   *
    manager.start(message.channel, {
      time: "1d", //one day
      prize: "Title",
      winnerNumber: 2, //2 winners
      interaction: message, //message of your command
      buttonType: Discord.ButtonStyle.Primary, //Style of button "participate"
    })

   */

  async start(channel: Channel, options: GiveawaysStartOptions): Promise<Message<boolean> | undefined>;
}




import { panel } from '../src/CompleteGiveaway/module/panel/manager/panel'
import { NewData } from "../src/CompleteGiveaway/module/panel/utils/Constant";


export declare class giveawaysPanel extends panel {

  constructor(client: Client) {
    super(client)
  }

  async panel(message: Message<boolean>, deprive: boolean): Promise<Message<boolean>>;
  async setConfig(message: Message<boolean>, options: NewData): Promise<Message<boolean>>;
}